<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css" />
    <!-- <script src="/js/index.js" defer></script> -->
    <title>Note</title>
  </head>
  <body>
    <img src="/img/logo.png" alt="logo" />
    <% if(user) { %>
    <h1 class="intro">Hi, <%= user[0].username %>, Here are your tasks.</h1>
      
    <form action="/users/note" method="post">
      <input type="text" name="tasks" class="create_task" required />
      <button type="submit" class="add">add</button>
    </form>
    <div class="tasks">
      <h2>Tasks</h2>
      <% if(todos.length > 0) {%> <% todos.forEach(todo => {%>
      <ul class="new_tasks">
        <form action=""></form>
        <li>
          <form action="" ><a href="" type="submit"><%=todo.tasks%></a></form>
        </li>
      </ul>
      <% }) %> <% } else { %>
      <p>You currently have not tasks</p>
      <%} %>
    </div>
    <form action="/users/logout" method="get">
      <button>Logout</button>
    </form>
  </body>
  <% } else { %>
  <form action="/users/logout" method="get">
    <button>Logout</button>
  </form>
  <% } %>
</html>
